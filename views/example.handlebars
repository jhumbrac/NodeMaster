<div class="container">
  <div class="row">
    <div class="col-8 offset-2">
      <div class="card mt-4">
        <div class="card-body">
          <h5 class="card-title">Example</h5>
          <p>
            <strong>ID</strong>: {{characters.id}}</p>
          <p>
          <p>
            <strong>Text</strong>: {{characters.name}}</p>
          <p>
            <strong>Description:</strong> {{characters.name}}</p>
            <a href="/">← Back To Home</a>
        </div>
      </div>
    </div>
  </div>
</div>
